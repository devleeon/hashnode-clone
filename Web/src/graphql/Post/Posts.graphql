query Posts($take: Int, $orderBy: [PostOrderByInput!], $skip: Int) {
  posts(
    where: { published: { equals: true } }
    take: $take
    orderBy: $orderBy
    skip: $skip
  ) {
    ...RegularPost
  }
}
