import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#2962ff",
    },
    secondary: {
      main: "#B7BABE",
      light: "#F4F7FA",
      dark: "#4C535D",
    },
  },
});

export default theme;
